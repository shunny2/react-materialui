export const Environment = {
  /**
   * Set the number of rows to load by default in lists.
   */
  LINE_LIMIT: 10,
  /**
   * Placeholder displayed in input.
   */
  INPUT_SEARCH: 'Search...',
  /**
   * Text is displayed when no record is found in a list.
   */
  EMPTY_LISTING: 'No records were found.',
  /**
   * URL of the project's database query base.
   */
  BASE_URL: 'http://localhost:3333/api/v1',
  /**
   * URL of the project's database query base.
   */
  JSON_SERVER_BASE_URL: 'http://localhost:3334'
};